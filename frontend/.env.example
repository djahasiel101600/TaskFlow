# TaskFlow backend URL. Must be the TaskFlow API server (e.g. Daphne on 9000).
# Do not point this at another app (e.g. paperless-ngx on 8000) or you will get 403 / wrong server.
VITE_API_URL=http://localhost:9000
